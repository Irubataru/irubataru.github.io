---
export interface Props {
  value: string
}

const {value} = Astro.props;
---

<span class="tag">{value}</span>

<style>
  .tag {
    background: var(--primary-color);
    color: white;
    padding: 0.3em 0.8em;
    border-radius: 15px;
    font-size: 0.85em;
    font-weight: 500;
  }
</style>
